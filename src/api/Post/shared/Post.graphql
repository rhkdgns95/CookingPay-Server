type Post {
    id: Int!

    title: String!
    description: String!
    photoUrls: [PostImage]

    donations: [Donation]
    writerId: Int
    writer: User!
    getAmounts: Int

    createdAt: String!
    updatedAt: String
}